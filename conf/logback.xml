<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="15 seconds" debug="false">
    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%d{ISO8601}]-[%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Reduce Jetty logging verbosity -->
    <logger name="org.eclipse.jetty" level="WARN" />
    <logger name="org.eclipse.jetty.io" level="WARN"/>
    <logger name="org.eclipse.jetty.util" level="WARN"/>
    <logger name="ch.qos.logback" level="WARN"/>
    <logger name="app" level="info"/>

    <!-- Jooby access log handler config -->
    <logger name="io.jooby.handler.AccessLogHandler" additivity="false" level="INFO">
        <appender-ref ref="STDOUT" />
    </logger>

    <!-- Root logger -->
    <root level="ERROR">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
